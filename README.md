# TASKERA_AI
TASKERA_AI is a stateful AI agent backend built with FastAPI and LangGraph. It supports persistent user sessions, RAG for document queries, and OCR for image text extraction. The agent autonomously uses multiple tools to answer complex requests. It also schedule your tasks such as meetings and events.
